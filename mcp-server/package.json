{
  "name": "kiro-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Kiro mode-specific prompt injection",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "kiro-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "install-global": "npm run build && npm link"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
